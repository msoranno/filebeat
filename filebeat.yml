filebeat.prospectors:
- input_type: log
  paths:
   # - /var/log/nginx/elk.log
    - /home/centos/logstash/temporal/bote.txt
    - /var/log/messages 
- input_type: log
  paths:
    - /home/centos/logstash/temporal/FST.txt
  document_type: fst

- input_type: log
  paths:
    - /home/centos/logstash/temporal/familia.txt
  document_type: familia

output.logstash:
  hosts: ["localhost:5043"]
